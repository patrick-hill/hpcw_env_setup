---
#- hosts: quantum
- hosts: localhost
  gather_facts: yes
  sudo: true
  vars:
    hostname:       quantum
    os_username:    'phill'
    common_install_media: false
    common_install_dev_utils: false

  pre_tasks:
    - shell: echo 'Starting...'
    
  roles:
    - common
    - nfs-mounts
    - what-pulse
    - chrome
    - vagrant
    - virtualbox
    # - ssh-server

  post_tasks:
    - shell: echo 'Done'

