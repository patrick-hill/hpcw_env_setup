---
- name: CouchPotato --> Check if installed
  stat: path=/apps/couchpotatoserver
  register: check_installed

- name: CouchPotato --> CentOS Install
  include: "centos.yml"
  when: ansible_distribution == 'CentOS' and check_installed.stat.exists == False