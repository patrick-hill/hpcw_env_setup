---
- name: Check if installed
  stat: path=/opt/packer/packer
  register: file_check

- name: Ubuntu install
  include: "ubuntu.yml"
  when: ansible_distribution == 'Ubuntu' and file_check.stat.exists == False