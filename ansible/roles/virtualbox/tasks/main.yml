---
- name: Virtualbox --> Check if installed
  command: dpkg-query -W virtualbox
  register: deb_check
  failed_when: deb_check.rc > 1
  changed_when: deb_check.rc == 1

- name: Virtualbox --> Ubuntu install
  include: "ubuntu.yml"
  when: ansible_distribution == 'Ubuntu' and deb_check.rc == 1