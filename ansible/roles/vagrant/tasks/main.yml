---
- name: Vagrant --> Check if installed
  command: dpkg-query -W vagrant
  register: deb_check
  failed_when: deb_check.rc > 1
  changed_when: deb_check.rc == 1

- name: Vagrant --> Ubuntu install
  include: "ubuntu.yml"
  when: ansible_distribution == 'Ubuntu' and deb_check.rc == 1